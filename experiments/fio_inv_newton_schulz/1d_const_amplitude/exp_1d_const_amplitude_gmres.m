exp_1d_const_amplitude_settings;

for it_p = 1 : num_n
    p = p_list(it_p);
    N = 2^p;

    fprintf("\n\n\n\n");
    fprintf("p: %d\n", p);

    result = run_FIO1D_inv_GMRES(...
        N, ...
        a_func, phi_func, ...
        n_leaf_bf, r_bf, tol_bf, type_bf, ...
        num_sample, ...
        restart_gmres, tol_gmres, maxit_gmres);

    save(data_path + "1d_const_amplitude_results_gmres" ...
        + "_" + string(p) ...
        + ".mat", "result", '-v7.3');
end

path(originalPath);
