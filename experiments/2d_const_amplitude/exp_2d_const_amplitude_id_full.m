exp_2d_const_amplitude_settings;

for it_tol_hss = 1 : num_tol_hss
    for it_p = 1 : num_n
        tol_hss = tol_hss_list(it_tol_hss);
        p = p_list(it_p);
    
        fprintf("\n\n\n\n");
        fprintf("p: %d\n", p);
   
        load("./data/2d_const_amplituide_results_cg" ...
            + "_" + string(p) ...
            + ".mat");
        f_ex = result.f_ex;
        Kf_ex = result.Kf_ex;
        n = result.n;

        result = [];
        result = run_FIO2D_inv_ID_Full(...
            exp_phi_func, n, ...
            r_bf, tol_bf, ...
            min_points, tol_hss, ...
            num_sample, ...
            tol_cg, maxit_cg, ...
            f_ex, Kf_ex);
        
        save("./data/1d_const_amplituide_results_id_full" ...
            + "_" + string(p) ...
            + "_" + string(tol_hss) ...
            + ".mat", "result");
    end
end

path(originalPath);
